spring:
  application:
    name: backend
  jackson:
    property-naming-strategy: SNAKE_CASE
  data:
    mongodb:
      uri: mongodb://admin:benuaadmin@mongodb:27017/benua?authSource=admin

server:
  port: 8080

app:
  cors:
    allowed-origins: http://localhost:3000
